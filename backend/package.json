{
  "name": "techshop-backend",
  "version": "1.0.0",
  "description": "Simple JSON Server Backend",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
